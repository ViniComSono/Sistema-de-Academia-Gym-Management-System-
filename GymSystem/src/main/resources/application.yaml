spring:
  application:
    name: GymSystem


  datasource:
    url: jdbc:mysql://localhost:3306/gymsystem
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}


  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true